<%= semantic_form_for :extract, :url => extract_admin_chain_url(params[:id]) do |f| %>
    <%= f.input :file, :as => :file, :label => false %>
    <%= f.actions do %>
        <%= f.action :submit, :as => :button %>
    <% end %>
<% end %>